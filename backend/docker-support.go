package main

/**
	Starting to add support for builds to run in docker containers instead of on the machine that is running the backend binary. Provides us with effective isolation
	and the ability for the user to not have the native compilers or build tools installed.
*/
func createContainer() string {
	return ""
}